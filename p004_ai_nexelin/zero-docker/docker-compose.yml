version: '3.8'

services:
  # Mock backend (працює!)
  mock-backend:
    build:
      context: .
      dockerfile: Dockerfile.mock
    ports:
      - "8787:8787"
    environment:
      PORT: 8787
      NODE_ENV: development

  # Frontend - використаємо локальний запуск поки що
  # frontend буде працювати локально на порту 3000
