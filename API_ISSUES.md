# 🔴 КРИТИЧНІ ПРОБЛЕМИ API - Потрібно виправити

## ❌ ВІДСУТНІ ENDPOINTS У DJANGO

### 1. Auth API (`/api/auth/`)
Фронтенд викликає, але Django не має:
- ❌ `POST /api/auth/register/` - Реєстрація
- ❌ `POST /api/auth/login/` - Вхід
- ❌ `POST /api/auth/logout/` - Вихід
- ❌ `GET /api/auth/me/` - Профіль користувача
- ❌ `POST /api/auth/refresh/` - Оновлення токена

**Рішення:**
1. Створити auth endpoints в Django
2. Або використовувати Django REST Framework Simple JWT (якщо встановлено)
3. Або змінити фронтенд на використання `/api/rag/auth/token-by-client-token/`

### 2. Legacy Agent API (опціонально)
Фронтенд має, але не використовує:
- ⚠️ `GET /api/agent/files/`
- ⚠️ `DELETE /api/agent/files/{id}/`
- ⚠️ `GET /api/agent/prompt/`
- ⚠️ `PUT /api/agent/prompt/`
- ⚠️ `POST /api/agent/train/`
- ⚠️ `GET /api/agent/train/status/`
- ⚠️ `GET /api/agent/history/`
- ⚠️ `GET /api/agent/history/{id}/`

**Рекомендація:** Видалити з `agentAPI` у фронтенді, якщо не потрібні.

---

## ✅ ВСІ ІНШІ API ПРАЦЮЮТЬ

Всі інші виклики мають відповідні ендпоінти в Django.

